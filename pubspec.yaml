name: countdown_app
description: تطبيق "كم باقي؟" للمناسبات.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  intl: 0.19.0
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2
  share_plus: ^9.0.0
  flutter_animate: ^4.5.0
  webview_flutter: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
